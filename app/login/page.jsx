import Login from "../components/pageComponents/Login";

export const metadata = {
  title: "Login",
  description: "Login, view, update and create entries",
};

export default function LoginPage() {
  return <Login />;
}
