import ViewEntry from "@/app/components/pageComponents/ViewEntry";

export const metadata = {
  title: "View",
  description: "view entry",
};

const ViewEntryPage = () => {
  return <ViewEntry />;
};

export default ViewEntryPage;
