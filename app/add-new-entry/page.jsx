import NewEntry from "../components/pageComponents/NewEntry";

export const metadata = {
  title: "Create new entry",
  description: "Create new entry",
};

const NewEntryPage = () => {
  return <NewEntry />;
};

export default NewEntryPage;
